# YouTube Report Generator - Requirements (Colab LLM Edition)
# =============================================================
# This file includes all dependencies for FULL LLM support.
# Optimized for Google Colab with GPU runtime.

# =============================================================
# Web Framework (install one based on your preference)
# =============================================================

# For Streamlit (with ngrok tunnel):
streamlit>=1.28.0

# For Gradio (with built-in share=True):
gradio>=4.0.0

# =============================================================
# YouTube API
# =============================================================
google-api-python-client>=2.100.0
isodate>=0.6.1

# =============================================================
# Language Detection
# =============================================================
langdetect>=1.0.9

# =============================================================
# Core ML Dependencies
# =============================================================
numpy>=1.24.0

# =============================================================
# LLM Dependencies (Required for FULL mode)
# =============================================================
# These are heavy but required for actual Llama 3.1-8B inference

torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0  # For 4-bit quantization (Linux/Colab only)

# =============================================================
# Optional: Tunnel for Streamlit in Colab
# =============================================================
pyngrok>=6.0.0

# =============================================================
# Utilities
# =============================================================
requests>=2.28.0

# =============================================================
# COLAB INSTALLATION NOTES:
# =============================================================
# 
# For Streamlit in Colab:
#   !pip install streamlit google-api-python-client isodate langdetect pyngrok
#   !pip install torch transformers accelerate bitsandbytes
#
# For Gradio in Colab:
#   !pip install gradio google-api-python-client isodate langdetect
#   !pip install torch transformers accelerate bitsandbytes
#
# Note: PyTorch is usually pre-installed in Colab, so you may not need
# to install it separately. Check with:
#   import torch
#   print(torch.__version__)
#   print(torch.cuda.is_available())
# =============================================================
